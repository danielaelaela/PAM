<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.introductorylab">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes">
        <activity android:name=".activity.EditNote"></activity>
        <activity
            android:name=".activity.Notebook"
            android:label="@string/title_activity_notebook"
            android:theme="@style/Theme.Notes.NoActionBar" />
        <activity
            android:name=".activity.EditNotebook"
            android:label="Create a notebook" />
        <activity
            android:name=".activity.NotebookCatalog"
            android:label="@string/title_activity_notebook_catalog"
            android:theme="@style/Theme.Notes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>