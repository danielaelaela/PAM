<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.introductorylab">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntroductoryLab">
        <activity
            android:name=".activity.NoteEditActivity"
            android:label="@string/title_activity_note_list"></activity>
        <activity
            android:name=".activity.NoteListActivity"
            android:label="@string/title_activity_note_list"
            android:theme="@style/Theme.IntroductoryLab.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>